# Gamemode
A gamemode consists of a set of rules that define how it behaves

Generic rules:
  * Board size
  * Point system
  * Dice faces, ex letters (standard), numbers (foggle)
  * Dice face distributions.
    * A Dice in boggle has 6 sides. 
    * Let the gamemode define the faces for each dice.
  
	* The mode package need to be able to send and receive messages between clients
